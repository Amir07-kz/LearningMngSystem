<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #f0f0f0;
        }
        form {
            width: 300px;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        input[type="email"], input[type="password"], input[type="submit"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 5px;
            box-sizing: border-box;
        }
        input[type="submit"] {
            background-color: #007bff;
            color: #fff;
            cursor: pointer;
        }
        input[type="submit"]:hover {
            background-color: #0056b3;
        }
        .error {
            color: red;
        }
    </style>
</head>
<body>
<form action="/login" method="post">
    <h2>Login</h2>
    {{ csrf_field() }}
    <input type="email" name="email" placeholder="Email">
    {% if errors.bags.default.messages.email %}
        <div class="error">{{ errors.bags.default.messages.email|first }}</div>
    {% endif %}
    <input type="password" name="password" placeholder="Password">
    {% if errors.bags.default.messages.password %}
        <div class="error">{{ errors.bags.default.messages.password|first }}</div>
    {% endif %}
    <input type="submit" value="Login">
</form>
</body>
</html>